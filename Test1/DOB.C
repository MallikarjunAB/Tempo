#include <stdio.h>
#include <conio.h>

void main()

{

int i,j,k=0;
printf("Enter your date only (DD) :");
scanf ("%d", &i);
printf("Enter your Month only (MM) :");
scanf ("%d", &j);
printf("Enter your Year only (YYYY) :");
scanf ("%d", &k);
printf(" your DOB is %d: %d : %d ",i,j,k);
getch();
}
